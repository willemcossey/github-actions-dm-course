name: print input at merge time

on:
  push:
    branches:
      - default

jobs:
  use-my-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: run my action
        uses: ./.github/actions/my-action

